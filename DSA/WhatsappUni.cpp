#include <iostream>
using namespace std;

struct Node *
{
    int val;
    struct Node *arr[];
}

int main()
{
    int n, m;
    cin >> n >> m;

    // n=no. of people
    // m=no. of groups
    int hero[n][m];

    for (int i = 0; i < m; i++)
    {
        int size;
        cin >> size;
        for (int j = 0; j < size; j++)
        {
            cin >> user_no;
            hero[i][j] = user_no;
        }
    }

    int hash[n];
    int visited[n];
    for (int i = 0; i < m; i++)

        return 0;
}